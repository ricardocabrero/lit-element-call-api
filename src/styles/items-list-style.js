import { css } from 'lit-element';

export const ItemListStyles = css`
    .list {
        background-color: rgb(0 0 0 / 25%);
        padding: 40px 1%;
        margin: 40px 0 0;
        display: flex;
        flex-wrap: wrap;
        list-style: none;
    }
`